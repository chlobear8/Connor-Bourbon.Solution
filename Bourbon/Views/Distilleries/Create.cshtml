@{
    Layout = "_Layout";
}

@model ConnorBourbon.Models.Distillery

<h4>Add a new Distillery</h4>
@using (Html.BeginForm())
{
    @Html.LabelFor(model => model.Name)
    @Html.TextBoxFor(model => model.Name)

    <input type="submit" value="Add a new distillery" class="btn btn-primary" />

}
<p>@Html.ActionLink("Show all categories", "Index")</p>